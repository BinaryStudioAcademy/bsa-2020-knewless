import { createRoutine } from 'redux-saga-routines';

export const checkFavouriteStateRoutine = createRoutine('CHECK_FAVOURITE');
export const changeFavouriteStateRoutine = createRoutine('CHANGE_FAVOURITE');
