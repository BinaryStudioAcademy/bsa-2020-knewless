import { createRoutine } from 'redux-saga-routines';

export const fetchGetStudentProfileRoutine = createRoutine('GET_STUDENT_PROFILE');
export const cleanFollowList = createRoutine('CLEAN_FOLLOW_LIST');
