import { createRoutine } from 'redux-saga-routines';

export const fetchStudentRoutine = createRoutine('DISCUSSION:FETCH_STUDENT');
export const fetchAuthorRoutine = createRoutine('DISCUSSION:FETCH_AUTHOR');
