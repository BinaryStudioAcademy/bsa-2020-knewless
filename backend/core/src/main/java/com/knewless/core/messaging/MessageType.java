package com.knewless.core.messaging;

public enum MessageType { // Hi developer, if you need to change this class, don't forget to change the same class at another end connection
    REQUEST, RESPONSE
}
